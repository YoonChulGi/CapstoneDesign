<!DOCTYPE html>
<html>
<head>
	<meta charset="UTF-8">
	<meta name="viewport" content="width=device-width">
	<title>Talent - Bank</title>
	<link rel="stylesheet" href="../css/bootstrap.css">
	<link rel="stylesheet" href="../css/codingBooster.css">
</head>
<%if(!req.user){%>
	<script>
		alert('로그인 한 유저만 이용할 수 있습니다.');
		window.location.href = '/login';
	</script>
<%}%>
<style>
	.btn-file {
    position: relative;
    overflow: hidden;
}
.btn-file input[type=file] {
    position: absolute;
    top: 0;
    right: 0;
    min-width: 100%;
    min-height: 100%;
    font-size: 100px;
    text-align: right;
    filter: alpha(opacity=0);
    opacity: 0;
    outline: none;
    background: white;
    cursor: inherit;
    display: block;
}
</style>
<body>
<h2>판매자의 재능을 4개 입력해주세요.</h2>
<form action="/registSellerPro" method="post" enctype="multipart/form-data">
	<div class="form-group col-sm-6">
		<div class="row">
			<p><input type="text" name="talent1" class="form-control"/></p>
		</div>
		<div class="row">
			<p><input type="text" name="talent2" class="form-control"/></p>
		</div>
		<div class="row">
			<p><input type="text" name="talent3" class="form-control"/></p>
		</div>
		<div class="row">
			<p><input type="text" name="talent4" class="form-control"/></p>
		</div>
		<%let provider = req.user.provider || req.user[0].provider; %>
		<%console.dir('provider = '+ provider);%>
		<%if(provider != 'kakao' ) {%>
		<h2>프로필 사진을 등록해 주세요</h2>
		<div class="col-xs-2">
   			<label class="input-group-btn">
    			<span class="btn btn-default btn-file">Browse <input accept=".gif, .jpg, .png" type="file" name="attach_file" data-display-target="attachFile" /></span>
   			</label>
   		</div>
   		<div class="col-xs-6">
   			<input type="text" class="form-control" readonly id="attachFile" placeholder="지원되는 파일 양식: jpg, png, gif" value="">
   		</div>
   		<%}%>
		<div class="row">
			<p><input type="submit" value="완료" class="form-control"/></p>
		</div>
	</div>
	
</form>
</body>
<script src="http://code.jquery.com/jquery-3.1.1.min.js"></script>
	<script src="../js/bootstrap.js"></script>
<script>
$(function() {
		 $("input[type='file']").on('change', function() {
			 $("#pictureflag").val('true');
		  var input = $(this);
		  var label = input.val().replace(/\\/g, '/').replace(/.*\//, '');
		  $("#" + input.data("display-target")).val(label);
		 });
	});

</script>

</html>
